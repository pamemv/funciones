/*  Transforma la siguiente función a una arrow function de una línea

let suma = function(a, b){
return a + b
}

nota: cuando se usan las {} debe ir un return, para que quede de 1 linea luego del => va directo la operación a realizar
*/

const suma => (a, b) => a + b


